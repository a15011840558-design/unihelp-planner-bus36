# Submission Evidence

This folder contains supporting evidence for the Agile Product Portfolio.

Recommended mapping:

| Story ID | Evidence |
| --- | --- |
| S1 | Source code in `app/routes.py`, `app/services.py`, tests in `tests/test_app.py`, workload screenshot |
| S2 | Resource finder implementation in `app/routes.py`, seeded resources in `app/db.py`, tests in `tests/test_app.py` |
| S3 | Wellbeing check-in implementation in `app/routes.py`, recommendation logic in `app/services.py`, tests in `tests/test_app.py` |

The prototype video is stored separately at `output/video/unihelp_prototype_demo.mp4`.

The remote Git repo link still needs to be added after the group uploads this project to GitHub/GitLab.
